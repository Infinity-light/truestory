{
  "name": "truestory-stt-proxy",
  "version": "1.0.0",
  "description": "WebSocket proxy that forwards browser STT traffic to Alibaba DashScope with Authorization header injection.",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "engines": {
    "node": ">=20"
  }
}
